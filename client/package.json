{
  "name": "@game/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@game/shared": "*",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  }
}
